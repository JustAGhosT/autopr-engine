[flake8]
# Minimal baseline that still parses; real ignores can be injected via env-selected config
max-line-length = 120
extend-ignore = E203,E266,E501,W503
per-file-ignores =
    tests/*:D101,D102
exclude =
    .venv,
    build,
    dist
max-complexity = 18